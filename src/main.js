import './styles.css';
import * as myModules from './myModules.js';

const DOMManipulator = (() => {
    /*Determine the main container*/
    const mainContainer = document.getElementById('content');

    console.log(myModules.homePageLoader);
})();